import 'package:flutter/material.dart';

class SettingsDevPage extends StatelessWidget {
  const SettingsDevPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Settings Dev Page'));
  }
}
