import 'package:flutter/material.dart';

class SettingsGeneralPage extends StatelessWidget {
  const SettingsGeneralPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Settings General Page'));
  }
}